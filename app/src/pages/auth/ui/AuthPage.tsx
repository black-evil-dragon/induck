import { AuthForm } from '@features/auth';
import React from 'react';

const AuthPage = () => {
    return (
        <div>
            AUTH PAGE
            <AuthForm />
        </div>
    );
}

export default AuthPage;
